```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  if(quantidade >= 12){
    return resultado = 1 * quantidade
  }else {
    return resultado = 1.3 * quantidade
  }
  console.log(resultado)
}
```